diff --git a/src/LibsslTLSContext.cc b/src/LibsslTLSContext.cc
index 2cf78323..58c21c9c 100644
--- a/src/LibsslTLSContext.cc
+++ b/src/LibsslTLSContext.cc
@@ -41,12 +41,35 @@
 #include <openssl/pkcs12.h>
 #include <openssl/bio.h>
 
+#ifdef __ANDROID__
+#  include <android/log.h>
+#  include <dirent.h>
+#  include <sys/stat.h>
+#  include <cerrno>
+#  include <climits>
+#  include <cstdio>
+#  include <openssl/pem.h>
+#  include <openssl/x509.h>
+#endif
+
 #include "LogFactory.h"
 #include "Logger.h"
 #include "fmt.h"
 #include "message.h"
 #include "BufferedFile.h"
 
+namespace aria2 {
+// aria2down extension: when SSL_CTX_new() fails (Android SECCOMP / DRBG
+// edge case, see CHANGELOG), the original error string lives only in
+// OpenSSL's error queue and is consumed immediately by ERR_get_error().
+// Stash a human-readable copy here so OpenSSLTLSSession::getLastErrorString
+// can surface it as `SSL initialization failed: <cause>` instead of an
+// empty colon. Single global is fine: aria2 holds one Platform/Session
+// across the process and we're only here to surface a fatal startup
+// fault.
+std::string g_aria2down_lastSslCtxNewError;
+} // namespace aria2
+
 namespace {
 struct bio_deleter {
   void operator()(BIO* b)
@@ -106,8 +129,24 @@ OpenSSLTLSContext::OpenSSLTLSContext(TLSSessionSide side, TLSVersion minVer)
   }
   else {
     good_ = false;
-    A2_LOG_ERROR(fmt("SSL_CTX_new() failed. Cause: %s",
-                     ERR_error_string(ERR_get_error(), nullptr)));
+    unsigned long err = ERR_get_error();
+    const char* causeStr = ERR_error_string(err, nullptr);
+    A2_LOG_ERROR(fmt("SSL_CTX_new() failed. Cause: %s", causeStr));
+    // Stash so OpenSSLTLSSession::getLastErrorString can surface this
+    // when the context is unusable and SSL_new(NULL) eventually fails.
+    if (causeStr != nullptr) {
+      g_aria2down_lastSslCtxNewError =
+          std::string("SSL_CTX_new failed: ") + causeStr;
+    }
+    else {
+      g_aria2down_lastSslCtxNewError = "SSL_CTX_new failed (no detail)";
+    }
+#ifdef __ANDROID__
+    // aria2 的 LogFactory 在 libaria2 FFI 模式下不会自动写入 Android logcat，
+    // 把 SSL_CTX_new 失败原因显式打到 logcat 便于现场诊断。
+    __android_log_print(ANDROID_LOG_INFO, "aria2down",
+                        "SSL_CTX_new failed: err=%lu cause=%s", err, causeStr);
+#endif
     return;
   }
 
@@ -275,6 +314,110 @@ bool OpenSSLTLSContext::addP12CredentialFile(const std::string& p12file)
 
 bool OpenSSLTLSContext::addSystemTrustedCACerts()
 {
+  // 上游 bug：若构造时 SSL_CTX_new 失败（在 Android 某些 ROM 上常见），
+  // sslCtx_ 仍为 nullptr，但本函数无防护地对其调 SSL_CTX_set_default_verify_paths，
+  // 直接段错误。补一道 null guard，让调用方收到 false 后能 graceful degrade。
+  if (!sslCtx_) {
+    A2_LOG_INFO("addSystemTrustedCACerts: SSL_CTX is null, skipping");
+    return false;
+  }
+#ifdef __ANDROID__
+  // aria2down patch (Android): OpenSSL 编译时 OPENSSLDIR 指向交叉编译机的
+  // 本地路径，Android 设备上不存在；SSL_CTX_set_default_verify_paths
+  // 加载不到任何根 CA → 所有 HTTPS 握手报 "unable to get local issuer
+  // certificate"。
+  //
+  // 关键陷阱：Android 系统 CA 目录里的 hash-named 文件
+  // （`/system/etc/security/cacerts/00673b5b.0` 这种）用的是 **OpenSSL
+  // 0.9.8 风格的旧 hash 算法** (`X509_NAME_hash_old`)，而 OpenSSL 1.0+
+  // 默认查找用的是新 hash (`X509_NAME_hash`)。所以
+  // `SSL_CTX_load_verify_locations(ctx, NULL, dir)` 在 Android CA 目录
+  // 上会"成功"返回 1 但运行时**找不到任何匹配 hash 的根 CA**，HTTPS 验证
+  // 仍然失败——这就是上一轮 7e4692c 修复实测没生效的原因。
+  //
+  // 正解：用 NDK 标准 POSIX <dirent.h> readdir() 遍历目录，对每个文件
+  // BIO_new_file → PEM_read_bio_X509 → X509_STORE_add_cert，**完全绕过**
+  // OpenSSL 的 hash 查找路径，直接把每张证书塞进 X509_STORE。验证时
+  // OpenSSL 会线性遍历 store 找 issuer，不依赖 hash 文件名。
+  //
+  // 路径优先级：
+  //   - /apex/com.android.conscrypt/cacerts/   Android 14 / API 34+
+  //   - /system/etc/security/cacerts/          Android 6+ / API 23+
+  //     （API 34+ 上是符号链接到 conscrypt APEX）
+  static const char* const kAndroidCADirs[] = {
+      "/apex/com.android.conscrypt/cacerts",
+      "/system/etc/security/cacerts",
+      nullptr,
+  };
+  X509_STORE* store = SSL_CTX_get_cert_store(sslCtx_);
+  int totalLoaded = 0;
+  const char* usedDir = nullptr;
+  for (const char* const* p = kAndroidCADirs; *p != nullptr; ++p) {
+    DIR* d = ::opendir(*p);
+    if (!d) {
+      __android_log_print(ANDROID_LOG_INFO, "aria2down",
+                          "opendir(%s) failed: %d", *p, errno);
+      continue;
+    }
+    int loadedHere = 0;
+    struct dirent* entry;
+    while ((entry = ::readdir(d)) != nullptr) {
+      // 跳过 "." / ".." 和隐藏文件。Android 上是 hash-named PEM
+      // （`00673b5b.0` 等），既不是隐藏文件也不是目录。保险起见用
+      // stat() 过滤掉非普通文件。
+      if (entry->d_name[0] == '.') continue;
+      char path[PATH_MAX];
+      int n = ::snprintf(path, sizeof(path), "%s/%s", *p, entry->d_name);
+      if (n < 0 || n >= static_cast<int>(sizeof(path))) continue;
+      struct stat st;
+      if (::stat(path, &st) != 0 || !S_ISREG(st.st_mode)) continue;
+      BIO* bio = BIO_new_file(path, "r");
+      if (!bio) {
+        ERR_clear_error();
+        continue;
+      }
+      // 单个文件可能含多张证书（罕见但合法）。循环读到 PEM_R_NO_START_LINE
+      // 即 EOF。
+      while (true) {
+        X509* cert =
+            PEM_read_bio_X509(bio, nullptr, nullptr, nullptr);
+        if (!cert) break;
+        if (X509_STORE_add_cert(store, cert) == 1) {
+          ++loadedHere;
+        }
+        // 注意：X509_STORE_add_cert 失败时通常是该证书已在 store 里
+        // （X509_R_CERT_ALREADY_IN_HASH_TABLE），不算错误；清掉避免污染
+        // error queue。
+        ERR_clear_error();
+        X509_free(cert);
+      }
+      BIO_free(bio);
+      // PEM_read_bio_X509 走到 EOF 也会 push PEM_R_NO_START_LINE 进 queue，
+      // 必须清掉，否则后续 SSL_get_error 会误报。
+      ERR_clear_error();
+    }
+    ::closedir(d);
+    if (loadedHere > 0) {
+      usedDir = *p;
+      totalLoaded = loadedHere;
+      break;
+    }
+  }
+  if (totalLoaded > 0) {
+    __android_log_print(ANDROID_LOG_INFO, "aria2down",
+                        "Loaded %d Android system CAs from %s", totalLoaded,
+                        usedDir);
+    A2_LOG_INFO(
+        fmt("Loaded %d Android system CAs from %s", totalLoaded, usedDir));
+    return true;
+  }
+  __android_log_print(
+      ANDROID_LOG_WARN, "aria2down",
+      "No Android system CA loadable from /apex or /system; HTTPS "
+      "verification will fail. Try --check-certificate=false as workaround.");
+  A2_LOG_INFO("No Android system CA loadable; falling back to OpenSSL "
+              "default verify paths (likely empty)");
+#endif
   if (SSL_CTX_set_default_verify_paths(sslCtx_) != 1) {
     A2_LOG_INFO(fmt(MSG_LOADING_SYSTEM_TRUSTED_CA_CERTS_FAILED,
                     ERR_error_string(ERR_get_error(), nullptr)));
diff --git a/src/LibsslTLSSession.cc b/src/LibsslTLSSession.cc
index 697029c3..8c4e6301 100644
--- a/src/LibsslTLSSession.cc
+++ b/src/LibsslTLSSession.cc
@@ -336,8 +336,24 @@ int OpenSSLTLSSession::tlsAccept(TLSVersion& version)
   return handshake(version);
 }
 
+// aria2down patch: stashed cause from a failed SSL_CTX_new() (see
+// LibsslTLSContext.cc). When ssl_ is NULL we cannot call SSL_get_error,
+// but we still want `SSL initialization failed: <cause>` to carry the
+// real reason instead of an empty colon.
+extern std::string g_aria2down_lastSslCtxNewError;
+
 std::string OpenSSLTLSSession::getLastErrorString()
 {
+  // ssl_ is NULL when SSL_new() failed in init() — usually because the
+  // owning OpenSSLTLSContext had SSL_CTX_new() return NULL during
+  // Platform::setUp. Surface the captured cause so the caller's
+  // `throw DL_ABORT_EX(fmt(EX_SSL_INIT_FAILURE, ...))` is actionable.
+  if (ssl_ == nullptr) {
+    if (!g_aria2down_lastSslCtxNewError.empty()) {
+      return g_aria2down_lastSslCtxNewError;
+    }
+    return "TLS context is not initialized";
+  }
   if (rv_ <= 0) {
     int sslError = SSL_get_error(ssl_, rv_);
     switch (sslError) {
diff --git a/src/Platform.cc b/src/Platform.cc
index 9f3ddb25..52d730af 100644
--- a/src/Platform.cc
+++ b/src/Platform.cc
@@ -45,6 +45,15 @@
 #  include <openssl/err.h>
 #  include <openssl/ssl.h>
 #  include "libssl_compat.h"
+#  if defined(__ANDROID__)
+#    include <openssl/crypto.h>
+#    include <openssl/rand.h>
+#    include <sys/syscall.h>
+#    include <unistd.h>
+#    include <fcntl.h>
+#    include <errno.h>
+#    include <android/log.h>
+#  endif
 #endif // HAVE_OPENSSL
 #ifdef HAVE_LIBGCRYPT
 #  include <gcrypt.h>
@@ -97,6 +106,55 @@ OSSL_PROVIDER* Platform::legacy_provider_ = nullptr;
 OSSL_PROVIDER* Platform::default_provider_ = nullptr;
 #endif // OPENSSL_VERSION_NUMBER >= 0x30000000L
 
+#if defined(HAVE_OPENSSL) && defined(__ANDROID__)
+namespace {
+// 直接从 /dev/urandom 抽熵。先用 syscall(SYS_getrandom) 绕过 libc 包装；若
+// SECCOMP 拦截则退到 read("/dev/urandom")。两条路径都不会触发 OpenSSL DRBG。
+int devurandom_bytes_impl(unsigned char* buf, int num)
+{
+  if (num <= 0) return 1;
+  size_t want = static_cast<size_t>(num);
+  size_t got = 0;
+#  ifdef SYS_getrandom
+  while (got < want) {
+    long n = ::syscall(SYS_getrandom, buf + got, want - got, 0);
+    if (n < 0) {
+      if (errno == EINTR) continue;
+      break;
+    }
+    if (n == 0) break;
+    got += static_cast<size_t>(n);
+  }
+  if (got == want) return 1;
+#  endif
+  int fd = ::open("/dev/urandom", O_RDONLY | O_CLOEXEC);
+  if (fd < 0) return 0;
+  while (got < want) {
+    auto n = ::read(fd, buf + got, want - got);
+    if (n < 0) {
+      if (errno == EINTR) continue;
+      break;
+    }
+    if (n == 0) break;
+    got += static_cast<size_t>(n);
+  }
+  ::close(fd);
+  return got == want ? 1 : 0;
+}
+
+int devurandom_status_impl() { return 1; }
+
+RAND_METHOD devurandom_method = {
+    nullptr,                 // seed
+    devurandom_bytes_impl,   // bytes
+    nullptr,                 // cleanup
+    nullptr,                 // add
+    devurandom_bytes_impl,   // pseudorand
+    devurandom_status_impl,  // status
+};
+} // namespace
+#endif // HAVE_OPENSSL && __ANDROID__
+
 Platform::Platform() { setUp(); }
 
 Platform::~Platform() { tearDown(); }
@@ -118,6 +176,53 @@ bool Platform::setUp()
 #endif // ENABLE_NLS
 
 #ifdef HAVE_OPENSSL
+#  if defined(__ANDROID__)
+  // 关键：在加载任何 provider 之前，禁用自动配置/atexit，并劫持 RAND_METHOD。
+  // 见 CHANGELOG.md「SimpleRandomizer.cc:121 assert(1 == rv)」条目：某些
+  // Android ROM 把 getrandom(2) SECCOMP 拦截后，OpenSSL 3.0 的 DRBG 永远
+  // 实例化失败，下游 RAND_bytes / SSL_CTX_new / peer-id 全连锁崩盘。
+  {
+    // ADD_ALL_CIPHERS / ADD_ALL_DIGESTS: 在 OpenSSL 3.0 上虽是 NOOP（已被
+    // provider model 替代），但加上保证如果将来动态链接到 1.1.1（Android
+    // 系统库）也能 fire 完整初始化路径。
+    int initRv = ::OPENSSL_init_crypto(
+        OPENSSL_INIT_NO_LOAD_CONFIG | OPENSSL_INIT_NO_ATEXIT
+            | OPENSSL_INIT_ADD_ALL_CIPHERS | OPENSSL_INIT_ADD_ALL_DIGESTS,
+        nullptr);
+    __android_log_print(ANDROID_LOG_INFO, "aria2down",
+                        "OPENSSL_init_crypto(no-config,no-atexit,add-all) "
+                        "-> %d",
+                        initRv);
+    // RAND_METHOD hook：让所有 legacy 路径（RAND_bytes / RAND_priv_bytes
+    // / SimpleRandomizer 等）经过 devurandom_bytes_impl。
+    int setRv = ::RAND_set_rand_method(&devurandom_method);
+    __android_log_print(ANDROID_LOG_INFO, "aria2down",
+                        "RAND_set_rand_method -> %d (devurandom)", setRv);
+    // 主动 seed：OpenSSL 3.0 SSL_CTX_new 内部走 RAND_priv_bytes_ex(libctx,
+    // ...)，可能转发到 default provider 的 EVP_RAND DRBG，而不是上面
+    // hook 的 method->bytes。先用 /dev/urandom 直读 256 字节高质量熵
+    // RAND_seed 进去，让 default DRBG 提前完成 instantiate，避开后续
+    // SSL_CTX_new 内部触发 getrandom() syscall（在 MIUI / 类似 SECCOMP
+    // 拦截设备上失败），让 HTTPS 下载在所有 Android 设备上稳定工作。
+    unsigned char seed_buf[256];
+    if (devurandom_bytes_impl(seed_buf, sizeof(seed_buf)) == 1) {
+      ::RAND_seed(seed_buf, sizeof(seed_buf));
+      __android_log_print(
+          ANDROID_LOG_INFO, "aria2down",
+          "RAND_seed(%zu bytes from /dev/urandom) OK", sizeof(seed_buf));
+    }
+    else {
+      __android_log_print(ANDROID_LOG_ERROR, "aria2down",
+                          "RAND_seed: failed to read /dev/urandom");
+    }
+    // 自检：试调一次 RAND_priv_bytes，失败时立刻 logcat 报警，方便用户
+    // 现场诊断（adb logcat -s aria2down）。
+    unsigned char test_buf[16];
+    int testRv = ::RAND_priv_bytes(test_buf, sizeof(test_buf));
+    __android_log_print(ANDROID_LOG_INFO, "aria2down",
+                        "RAND_priv_bytes self-test -> %d (1=OK)", testRv);
+  }
+#  endif // __ANDROID__
 #  if OPENSSL_VERSION_NUMBER >= 0x30000000L
   // RC4 is in the legacy provider.
   legacy_provider_ = OSSL_PROVIDER_load(nullptr, "legacy");
diff --git a/src/SimpleRandomizer.cc b/src/SimpleRandomizer.cc
index f1603935..fe38ee4b 100644
--- a/src/SimpleRandomizer.cc
+++ b/src/SimpleRandomizer.cc
@@ -36,6 +36,8 @@
 
 #include <sys/types.h>
 #include <unistd.h>
+#include <fcntl.h>
+#include <cerrno>
 #include <cstdlib>
 #include <cassert>
 #include <cstring>
@@ -118,6 +120,26 @@ void SimpleRandomizer::getRandomBytes(unsigned char* buf, size_t len)
 #elif defined(HAVE_OPENSSL)
   auto rv = RAND_bytes(buf, len);
   if (rv != 1) {
+#  if defined(__ANDROID__) || defined(__linux__)
+    // 兜底保险：OpenSSL DRBG 在某些 Android ROM 上无法初始化（SECCOMP 拦截
+    // getrandom(2)，OpenSSL rand_unix.c 不会再退到 /dev/urandom 直读），导致
+    // RAND_bytes 全部返回 0，进而 peer-id 生成 abort。直接 read /dev/urandom
+    // 在 Android/Linux 上是受支持的 CSPRNG 入口，性能可忽略。
+    int fd = ::open("/dev/urandom", O_RDONLY | O_CLOEXEC);
+    if (fd >= 0) {
+      size_t got = 0;
+      while (got < len) {
+        auto n = ::read(fd, buf + got, len - got);
+        if (n <= 0) {
+          if (n < 0 && errno == EINTR) continue;
+          break;
+        }
+        got += static_cast<size_t>(n);
+      }
+      ::close(fd);
+      if (got == len) return;
+    }
+#  endif // __ANDROID__ || __linux__
     assert(1 == rv);
     abort();
   }
